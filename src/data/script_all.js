// const qblocks = document.querySelectorAll('.qblock');

// const result = Array.from(qblocks).map((qblock) => {
//   const basisElement = qblock.querySelector('.Basis');
//   const basisText = basisElement ? basisElement.textContent.trim() : '';

//   const distractorsTable = qblock.querySelector('.distractors-table');
//   let options = [];

//   if (distractorsTable) {
//     const msoElements = distractorsTable.querySelectorAll('.active-distractor p');
//     options = Array.from(msoElements).map((el) => ({
//       text: el.textContent.trim()
//     }));
//   }

//   return {
//     text: basisText,
//     options
//   };
// });

// console.log(result)
